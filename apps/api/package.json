{
  "name": "@autoboard/api",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@autoboard/db": "workspace:*",
    "@autoboard/domain": "workspace:*",
    "@autoboard/logger": "workspace:*",
    "@autoboard/services": "workspace:*",
    "@autoboard/shared": "workspace:*",
    "@hono/node-server": "^1.13.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@autoboard/testing": "workspace:*",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0",
    "vitest": "^2.1.0"
  }
}
